{
  "name": "page-test",
  "version": "1.0.0",
  "description": "Auto deploy code to GH Pages",
  "main": "src/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js"
  }
}
